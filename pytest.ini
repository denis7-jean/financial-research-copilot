[pytest]
asyncio_mode = auto
pythonpath = .
markers =
    llm: marks tests that make live LLM calls to Bedrock (requires AWS credentials)
    integration: marks tests that require uvicorn running on port 8000
